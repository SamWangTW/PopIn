// Re-uses the CreateEventScreen component for edit mode.
// Registered as a non-tab route (href: null) so navigating here
// pushes a stack screen rather than switching to the Create tab.
export { default } from "./create";
